# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
web-report/
.metro-health-check*

# native projects (never prebuilt here — Expo Go + EAS Update only)
ios/
android/

# credentials & signing — never commit
*.jks
*.p8
*.p12
*.key
*.pem
*.mobileprovision
.env*

# Python (server/komfy-listing extension)
__pycache__/
*.pyc

# macOS
.DS_Store

# logs & misc
*.log
npm-debug.*
*.orig.*
*.tsbuildinfo

# supervisor secret (auth token, generated at first run — never commit)
server/supervisor/.token

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# docs/ — local working notes (API captures, workflow plans): never published.
# Exception: the architecture diagram embedded in the README §Architecture.
docs/*
!docs/architecture.excalidraw
!docs/architecture.png
!docs/architecture-dark.png
